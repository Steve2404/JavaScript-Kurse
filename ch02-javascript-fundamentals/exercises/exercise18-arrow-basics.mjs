/**
 * EXERCICE 18 - Kleine Aufgaben mit Arrow Functions lösen
 * =======================================================
 *
 * Niveau : mittel
 *
 * Hauptziel (= objectif principal) : Arrow Functions, Parameterklammern und implizite Rückgabe (= retour implicite)
 *
 * -- Rappel du decoupage en "boites magiques" --
 * Eine Funktion ist wie eine magische Box (= boîte magique) : Sie bekommt Zutaten
 * (= ingrédients / Parameter), macht eine klar begrenzte Arbeit und kann ein Ergebnis
 * zurückgeben. Bei jeder Teilaufgabe frage dich: Hat sie eine eigene kleine Aufgabe?
 * Kommt sie mehrfach vor? Versteckt sie eine eigene kleine Regel?
 *
 * ==================================================================
 * TODO - Kleine Aufgaben mit Arrow Functions lösen
 * ==================================================================
 *
 * -- Das Problem, wie für ein kleines Kind erklärt (= problème expliqué simplement) --
 * Ein kleines Preisprogramm braucht drei kurze Funktionen: verdoppeln, zwei Werte addieren und einen festen Systemnamen liefern.
 *
 * -- Probieren wir es zuerst von Hand (= essayons d'abord à la main) --
 * double(4) -> 8
 * add(2,3) -> 5
 * systemName() -> "ITL"
 *
 * -- Der Plan (= le plan) --
 * 1. Schreibe double mit genau einem Parameter ohne Klammern.
 * 2. Schreibe add mit zwei Parametern und Klammern.
 * 3. Schreibe systemName ohne Parameter mit ().
 * 4. Nutze bei allen drei die implizite Rückgabe.
 *
 * -- Braucht dieser Plan eine eigene magische Box? --
 * Ja. Jede kurze Berechnung ist eine eigene kleine Box.
 *
 * Bevor du Node.js startest: Schreibe zuerst deine erwarteten Zwischenwerte
 * (= valeurs intermédiaires attendues) auf Papier. Danach erst ausführen.
 *
 * Technische Hinweise (= indices techniques) :
 * - Ein einfacher Parameter darf ohne Klammern stehen.
 * - Null oder mehrere Parameter brauchen Klammern.
 * - Ohne Blockklammern wird der Ausdruck automatisch zurückgegeben.
 */

import { check, summary } from "../exerciseChecker.mjs";

// TODO: double, add und systemName als Arrow Functions implementieren.
throw new Error("TODO: Arrow Functions implementieren");
